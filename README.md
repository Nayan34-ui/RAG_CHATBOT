# RAG_CHATBOT
This project implements a RAG chatbot using LLM to deliver accurate, context-aware answers from external knowledge sources. It combines semantic search with generative AI, ensuring that responses are both fact-grounded and natural.
